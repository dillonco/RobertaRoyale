/**
 * config.js — Runtime configuration.
 * In production, set WS_URL to your WebSocket server (e.g. 'wss://euchre-server.fly.dev').
 * Leave as null for local development — network.js falls back to same-origin.
 */
window.WS_URL = null;
